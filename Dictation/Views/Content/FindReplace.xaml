<Page
    x:Class="Dictation.Views.Content.FindReplace"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Dictation.Views.Content"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
     Width="300">

    <StackPanel Margin="10">
        <!--<TextBlock TextWrapping="Wrap" Margin="0 0 30 10">Find every instance of a word in your document and replace it with another word.</TextBlock>
        <TextBlock FontSize="20" FontWeight="SemiBold">Find what:</TextBlock>
        <TextBox Margin="5" Text="{x:Bind FindReplaceLogic.SearchedWord, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"></TextBox>
        <CheckBox Margin="5 0 0 0" IsChecked="{x:Bind FindReplaceLogic.IsMatchCase, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">Match case</CheckBox>
        <Button Height="30" Click="{x:Bind FindReplaceLogic.FindNext}">Find Next</Button>
        
        <TextBlock FontSize="20" FontWeight="SemiBold">Replace with:</TextBlock>
        <TextBox Margin="5" Text="{x:Bind FindReplaceLogic.SearchedWord, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"></TextBox>
        <StackPanel Orientation="Horizontal">
            <Button Height="30" Click="{x:Bind FindReplaceLogic.Replace}">Replace</Button>
            <Button Height="30" Margin="20 0 0 0" Click="{x:Bind FindReplaceLogic.ReplaceAll}">Replace All</Button>
        </StackPanel>-->
    </StackPanel>

</Page>

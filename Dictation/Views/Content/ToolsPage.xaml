<Page
    x:Class="Dictation.Views.Content.ToolsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:i="using:Microsoft.Xaml.Interactivity"
    xmlns:ic="using:Microsoft.Xaml.Interactions.Core"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
     Width="300">
    <Page.Resources>
        <Style TargetType="ToggleButton">
            <Setter Property="Width" Value="30"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="FontSize" Value="10"/>
        </Style> 
        <Style TargetType="Button">
            <Setter Property="Width" Value="30"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="FontSize" Value="10"/>
        </Style>
    </Page.Resources>

    <StackPanel Margin="10">
        <TextBlock Margin="5" FontSize="20" FontWeight="SemiBold">Select font style</TextBlock>
        <StackPanel Orientation="Horizontal" Height="40">
            <ComboBox Margin="5" Width="150" ItemsSource="{x:Bind Fonts}" SelectedItem="{x:Bind viewModel.Font, Mode=TwoWay}">
                <i:Interaction.Behaviors>
                    <ic:EventTriggerBehavior EventName="SelectionChanged">
                        <ic:InvokeCommandAction Command="{x:Bind viewModel.FontCommand}" CommandParameter="{x:Bind viewModel.Font, Mode=OneWay}" />
                    </ic:EventTriggerBehavior>
                </i:Interaction.Behaviors>
            </ComboBox>
            <ComboBox Margin="10 5 5 5" ItemsSource="{x:Bind Sizes}" SelectedItem="{x:Bind viewModel.Size, Mode=TwoWay}">
                <i:Interaction.Behaviors>
                    <ic:EventTriggerBehavior EventName="SelectionChanged">
                        <ic:InvokeCommandAction Command="{x:Bind viewModel.SizeCommand}" CommandParameter="{x:Bind viewModel.Size, Mode=OneWay}" />
                    </ic:EventTriggerBehavior>
                </i:Interaction.Behaviors>
            </ComboBox>
            
        </StackPanel>
        <VariableSizedWrapGrid MaximumRowsOrColumns="5" Orientation="Horizontal">
            <ToggleButton FontFamily="Segoe MDL2 Assets" Content="&#xE8DD;" IsChecked="{x:Bind viewModel.IsBold, Mode=OneWay}" Command="{x:Bind viewModel.StyleCommand}" CommandParameter="Bold" ></ToggleButton>
            <ToggleButton FontFamily="Segoe MDL2 Assets" Content="&#xE8DB;" IsChecked="{x:Bind viewModel.IsItalic, Mode=OneWay}" Command="{x:Bind viewModel.StyleCommand}" CommandParameter="Italic"></ToggleButton>
            <ToggleButton FontFamily="Segoe MDL2 Assets" Content="&#xE8DC;" IsChecked="{x:Bind viewModel.IsUnderline, Mode=OneWay}" Command="{x:Bind viewModel.StyleCommand}" CommandParameter="Underline"></ToggleButton>
            
            <ToggleButton FontFamily="Segoe MDL2 Assets" Content="&#xE8D3;"></ToggleButton>
            <ToggleButton FontFamily="Segoe MDL2 Assets" Content="&#xE006;"></ToggleButton>
            <ToggleButton IsChecked="{x:Bind viewModel.IsStrikethrough, Mode=OneWay}" Command="{x:Bind viewModel.StyleCommand}" CommandParameter="Strikethrough">
                <Grid>
                    <FontIcon FontSize="10"  FontFamily="Segoe MDL2 Assets" Glyph="&#xE8E9;" />
                    <FontIcon FontSize="10" FontFamily="Segoe MDL2 Assets" Glyph="&#xE921;" />
                </Grid>
            </ToggleButton>
            <ToggleButton Content="X&#x2082;" IsChecked="{x:Bind viewModel.IsSubscript, Mode=OneWay}" Command="{x:Bind viewModel.StyleCommand}" CommandParameter="Subscript"></ToggleButton>
            <ToggleButton Content="X²" IsChecked="{x:Bind viewModel.IsSuperscript, Mode=OneWay}" Command="{x:Bind viewModel.StyleCommand}" CommandParameter="Superscript"></ToggleButton>
            <ComboBox  Width="70" Margin="5" VariableSizedWrapGrid.ColumnSpan="2"></ComboBox>
        </VariableSizedWrapGrid>
        <TextBlock Margin="5" FontSize="20" FontWeight="SemiBold">Set alignment</TextBlock>
        <StackPanel Orientation="Horizontal">
            <ToggleButton FontFamily="Segoe MDL2 Assets" Content="&#xE8E4;" IsChecked="{x:Bind viewModel.IsAlignLeft, Mode=OneWay}"  Command="{x:Bind viewModel.StyleCommand}" CommandParameter="AlignLeft"></ToggleButton>
            <ToggleButton FontFamily="Segoe MDL2 Assets" Content="&#xE8E3;" IsChecked="{x:Bind viewModel.IsAlignCenter, Mode=OneWay}"  Command="{x:Bind viewModel.StyleCommand}" CommandParameter="AlignCenter"></ToggleButton>
            <ToggleButton FontFamily="Segoe MDL2 Assets" Content="&#xE8E2;" IsChecked="{x:Bind viewModel.IsAlignRight, Mode=OneWay}"  Command="{x:Bind viewModel.StyleCommand}" CommandParameter="AlignRight"></ToggleButton>
            <ToggleButton IsChecked="{x:Bind viewModel.IsJustify, Mode=OneWay}" Command="{x:Bind viewModel.StyleCommand}" CommandParameter="Justify">
                <Grid>
                    <FontIcon FontSize="10"  FontFamily="Segoe MDL2 Assets" Glyph="&#xE8E4;" />
                    <FontIcon FontSize="10" FontFamily="Segoe MDL2 Assets" Glyph="&#xE8E2;" />
                </Grid>
            </ToggleButton>
        </StackPanel>
        <TextBlock Margin="5" FontSize="20" FontWeight="SemiBold">Bullets and Numbering</TextBlock>
        <StackPanel Orientation="Horizontal">
            <ToggleButton IsChecked="{x:Bind viewModel.HasBullets, Mode=OneWay}" FontFamily="Segoe MDL2 Assets" Content="&#xE8FD;" Command="{x:Bind viewModel.StyleCommand}" CommandParameter="Bullets"></ToggleButton>
            <ToggleButton IsChecked="{x:Bind viewModel.HasNumbers, Mode=OneWay}" Command="{x:Bind viewModel.StyleCommand}" CommandParameter="Numbers"></ToggleButton>
        </StackPanel>
        <TextBlock Margin="5" FontSize="20" FontWeight="SemiBold">Line spacing</TextBlock>
        <StackPanel Orientation="Horizontal">
            <ToggleButton></ToggleButton>
            <ToggleButton></ToggleButton>
            <ToggleButton></ToggleButton>
            <ToggleButton></ToggleButton>
            <ToggleButton></ToggleButton>
            <ToggleButton></ToggleButton>
        </StackPanel>
        <TextBlock Margin="5" FontSize="20" FontWeight="SemiBold">Insert</TextBlock>
        <StackPanel Orientation="Horizontal">
            <Button></Button>
            <Button></Button>
        </StackPanel>
    </StackPanel>
</Page>
